# サイト・ブラッシュアップ提案

対象: `dezako/index.html`（公立はこだて未来大学 情報デザインコース 特設サイト）

「パフォーマンス・アセット」「アクセシビリティ・セマンティクス」「UX・コンテンツ整合性」の3観点で全体を洗い出した結果をまとめる。各項目は「現状」→「提案」の形式。行番号は調査時点のもの。

---

## 最優先（来場者・公開に直接影響する）

### 1. フッターのリンクが全てダミー、連絡先がサイト上に一切ない
- **現状**: `site-footer-links`（1983-1985行）の「大学公式サイト」「お問い合わせ」「アクセス」が全て `href="#"`。`mailto:`/`tel:` もファイル内に0件。
- **提案**: 大学公式サイトURL・問い合わせ先（メール or 電話 or フォームリンク）・アクセスページへの実リンクを設定する。特に「お問い合わせ」導線の欠落は、興味を持った来場者が連絡できないという実害に直結する。

### 2. 日付表記の矛盾（8/3 vs 8/1）
- **現状**: `#section-welcome` 内の一文（1850行）「8/3オープンキャンパスでも、デザコ ブースにて詳しくお話します」が「8/3」表記。一方、CTAセクションのポスターalt（1941行）・イベント詳細欄（1949行）は「8月1日（土）」で統一。2026年8月1日は実際に土曜日（8/3は月曜）のため、CTA側が正、1850行が誤記の可能性が高い。
- **提案**: 1850行を「8/1」に修正（単純な日付タイポとみられる）。

### 3. 診断結果ポスター画像6枚が重すぎる（合計約9.7MB）
- **現状**: 部活勧誘ポスター.png 等6枚は実解像度1024×1536pxのPNGだが、実際の表示は `.poster-frame-wrap`（272×368px）内でしか使われず、約3.7倍の無駄な解像度＋可逆圧縮PNGで合計約9.7MBを消費。診断ゲームクリア時に読み込む画像のため、モバイル回線だと表示ラグの原因になりうる（先読み処理は既にあるが、画像自体を軽量化すればさらに改善する）。
- **提案**: 272×368px相当（Retina考慮でも600×800px程度で十分）にリサイズし、JPEGまたはWebPに変換する。

### 4. ポスター制作のチップがキーボード・スクリーンリーダー非対応
- **現状**: `.chip`（1994・2335行）は `<div>` に `pointerdown` イベントのみを付与したドラッグ&ドロップ要素。`role`・`tabindex`・`keydown` ハンドラは一切なく、マウス/タッチ操作以外の手段でゲームを完了できない。
- **提案**: 最低限、キーボードでチップを選択→配置先を選択して確定できる代替操作（矢印キー＋Enter等）を用意するか、スクリーンリーダー向けに操作方法を `aria-live` 等で案内する。工数が大きい場合は既知の制約として認識しておく。

---

## 中優先（品質・体験を底上げする）

### 5. 画像に `loading="lazy"` が一切ない
- **現状**: `<img>` 23個すべてに `loading`/`decoding` 属性なし。facility写真（工房・アトリエ・ミュージアム、1916-1926行）等ファーストビュー外の画像も初期表示時に一括ロードされる。
- **提案**: ファーストビュー外の `<img>`（facility系、assignment系）に `loading="lazy" decoding="async"` を追加。診断結果画像はJS動的生成＋既存の `preloadAllPosterImages()` があるため対象外でよい。

### 6. facility写真・kv写真がPNGかつ表示サイズに対して過大
- **現状**: 工房.JPG等は900×675pxだが実表示は `.facility-img` の高さ80pxのサムネイルのみ。kv-photo-1〜4.pngは780px幅で妥当だが写真をPNG保存しておりJPEG/WebPより重い。
- **提案**: facility系は200×150px程度にリサイズ、kv-photo系はJPEG/WebP化。

### 7. 診断結果・課題紹介画像のalt属性が内容説明になっていない
- **現状**: `overlay.innerHTML` で生成される診断結果画像（2629行）は `alt="${d.type}"`（診断タイプ名をそのまま使用）、課題紹介画像（2769行）も `alt="${a.title}"`。スクリーンリーダーでは「画像」であることは伝わるが、構図やビジュアルの説明にはなっていない。
- **提案**: 必須ではないが、時間があれば簡単な説明文をalt化する。最低限、装飾目的でないことは示せているため優先度は中程度。

### 8. 「実験データをコピー」ボタンが一般公開ページに常時表示
- **現状**: `#export-log-btn`（1970行）はCTAセクションの「参加を申し込む」直下に、他の正規CTAボタンと同格の見た目（角丸・ボーダー・box-shadow共通）で常時表示されている。出し分けフラグ等は見つからず。
- **提案**: 意図的な実験用機能であれば、一般来場者の目に触れない形（クエリパラメータでのみ表示、開発者ツールのコンソールコマンド化等）に変更するか、公開版では非表示にすることを検討。すでにこの用途で使っているなら現状維持でよい。

### 9. `--text-muted`（55%不透明度の暗赤）のコントラスト不足の懸念
- **現状**: `--text-muted: rgba(122, 0, 38, 0.55)` が `--page-bg: #FFF2EF` 背景の上で注釈・補足テキストに多用されている（215, 515, 1088, 1096, 1140, 1177, 1183, 1201, 1352, 1397, 1480, 1510, 1524, 1531行など）。目視でコントラスト比はWCAG AA基準（4.5:1）を下回る可能性が高い。
- **提案**: 不透明度を70-80%程度に上げるか、`--text-muted` 用に別途コントラスト確保した色を定義する。

---

## 低優先（気づいたら直す程度）

### 10. `fonts.gstatic.com` へのpreconnectが欠落
- 8-9行目。追加で往復1回分の読み込み短縮が見込める。

### 11. 死んだCSSセレクタが残存
- 例: `.center-card-header .sub-label`（213行）。整理すると保守しやすくなる。

### 12. ボタンの矢印表記ゆれ
- 「つくる →」「次へ ↓」「シェアする ↗」等は矢印付きだが「もう一度試す」（2544・2599行）だけ矢印なし。統一すると細部の一貫性が上がる。

### 13. 全角/半角数字の表記ゆれ
- `ASSIGNMENTS` 内 `desc`（2105行）の「大学**1**年生」が半角、他は「２年生」等全角。統一が望ましい。

### 14. `<h1>` が存在しない・見出し階層が一部飛んでいる
- `<h3>` が対応する `<h2>` なしで登場（1845行付近）。SEO・スクリーンリーダーのナビゲーション性向上のため是正が望ましい。

### 15. `<main>` 等ランドマーク要素がなく構造が `<div>` の羅列
- 大規模な構造変更になるため優先度は低いが、将来的な保守性のために検討の余地あり。

### 16. `prefers-reduced-motion` 未対応
- フェードイン・スライドイン・PDCA回転・ベン図アニメーション等、多数のCSSアニメーションがあるが動き軽減設定への配慮がゼロ。該当ユーザー向けに `@media (prefers-reduced-motion: reduce)` でアニメーションを簡略化する余地あり。

### 17. viewportの `user-scalable=no`
- ピンチズームを禁止しており低視力ユーザーに不利。ドラッグ操作との両立が理由と推測されるが、`touch-action` をドラッグ要素個別に指定すればズーム許可との共存も可能。変更にはUXへの影響検証が必要なため低優先。

### 18. レスポンシブ対応がカード幅の拡大のみ
- 600px/900px時に `#screen-top` 等の `max-width` が広がるが、`.facility-img`（80px固定）・`.tool-icon`（36px固定）・`.assign-card-row`（常に縦1列）等の内部要素は追従せず、広い画面では「小さいアイコン＋広い余白」で間延びする懸念。タブレット/PC幅での実際の見た目を確認した上で必要なら調整。
